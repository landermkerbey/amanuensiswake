#+TITLE: Amanuensis Wake — Implementation Plan
#+AUTHOR: (your name)
#+DATE: 2026-05-07
#+PROPERTY: session amanuensiswake
#+STARTUP: overview

* Context for AI sessions

This document is the canonical plan for building the Amanuensis Wake personal
writing website. Pass it at the start of any AI session to establish context.

** Project summary

- *Site title*: Amanuensis Wake
- *Domain*: amanuensiswake.com
- *Code repository*: =amanuensiswake= (public GitHub repo)
- *Content repository*: =amanuensiswake-content= (separate public GitHub repo, git submodule at =content/=)
- *Stack*: TypeScript static site generator, nginx on Digital Ocean VPS (Ubuntu 24.04)
- *Authoring format*: Emacs Org-mode (=.org= files)
- *Domain registrar*: Hover; DNS proxied through Cloudflare
- *Development methodology*: TDD — smallest working chunks, failing tests first

** Key constraints

1. No Haskell. The SSG is TypeScript-native.
2. Org-mode is the *only* authoring format.
3. The engine supports AST-level transforms for future features (backlinks,
   sidenotes, transclusion, etc.).
4. Content lives in a separate public repository, connected as a git submodule.
   Publication is controlled by what is committed and pushed to that repo.

** Technology decisions (confirmed)

| Concern          | Choice                                     |
|------------------+--------------------------------------------|
| Language         | TypeScript                                 |
| Test runner      | vitest                                     |
| TS runner        | tsx                                        |
| Org parser       | uniorg (unified.js-compatible)             |
| File watching    | chokidar v3                                |
| Server           | nginx                                      |
| DNS              | Hover → Cloudflare (proxied) → VPS         |

** Current codebase structure

#+BEGIN_EXAMPLE
amanuensiswake/
├── src/
│   ├── parser/index.ts             ← parse() — title, date, tags, bodyHtml, rawContent
│   ├── builder/
│   │   ├── index.ts                ← build({ contentDir, outputDir, transforms?, publicDir? })
│   │   └── collect-entries.ts      ← collectEntries() — reads #+DATE / #+CREATED, #+FILETAGS
│   ├── rehype-verse.ts             ← rehype plugin: wraps verse blocks in .aw-verse
│   ├── transforms/
│   │   ├── types.ts                ← Transform, BuildContext, OrgData, visitNodes
│   │   └── index.ts                ← public re-exports
│   ├── server/
│   │   ├── watch.ts                ← startWatcher({ contentDir, outputDir })
│   │   └── serve.ts                ← createServer(distDir, port)
│   └── dev.ts                      ← entry point for `npm run dev`
├── content/                        ← git submodule (amanuensiswake-content)
├── dist/                           ← build output, gitignored
├── public/
│   └── style.css                   ← all site styles (design-token–driven, dark mode)
├── templates/
│   ├── base.ts                     ← baseTemplate(TemplateData) — home + article layouts
│   └── index-template.ts           ← indexTemplate(ledeHtml, entries) — home page body
├── deploy/
│   ├── nginx.conf                  ← symlinked from /etc/nginx/sites-available/
│   └── README.org                  ← deploy, server setup, and content update workflow
├── tests/
│   ├── smoke.test.ts
│   ├── fixtures/
│   ├── parser/parse.test.ts
│   ├── builder/build.test.ts
│   ├── builder/collect-entries.test.ts
│   ├── server/watch.test.ts
│   └── transforms/pipeline.test.ts
├── package.json                    ← type: module, scripts: test/dev/build/typecheck
├── tsconfig.json
└── vitest.config.ts
#+END_EXAMPLE

** Known behaviours and gotchas

- =index.org= outputs to =dist/index.html= (not =dist/index/index.html=).
  It is processed by =processIndex=, which uses =indexTemplate= to compose the
  lede + entry list, then passes the result to =baseTemplate= with =isHome: true=.
- =baseTemplate= has two layout modes controlled by =isHome: boolean=:
  - *Home*: header gets =aw-site-header--home= (1.35rem site name); body renders
    directly in the column with no =<article>= wrapper, =<h1>=, dateline, or tags.
  - *Article*: standard layout with =<article>=, =<h1>=, dateline, and tag pills.
- =collectEntries= reads =#+DATE= first, then falls back to =#+CREATED= if
  absent. Content files may use either keyword; the index date column uses both.
- Content before any heading in an org file sits directly on the =org-data= AST
  node. Content under a heading is wrapped in a =section= node. Transforms must
  account for both.
- The uniorg AST type is imported directly from the =uniorg= package. Do not
  redefine =OrgData= locally — it will drift from the library type.
- chokidar must be pinned to v3. v4 and v5 have breaking API changes and do not
  fire events reliably in test environments.
- The deploy user is =lander=, who is a member of the =amanuensiswake= group.
  The deploy directory =/var/www/amanuensiswake/dist= must have =g+w= set for
  rsync to succeed.
- rsync deploy command: =rsync -avz --no-group --delete dist/ lander@SERVER_IP:/var/www/amanuensiswake/dist/=
- Content update workflow: commit + push to =amanuensiswake-content=; then in
  code repo =git submodule update --remote content && git add content && git commit=;
  CI/CD handles the rest automatically.

* Completed phases (condensed)
:PROPERTIES:
:STATUS: complete
:END:

- *Phase 0* — Repo + tooling bootstrap. TypeScript, vitest, tsx, esbuild. Smoke test passes.
- *Phase 1* — Org-file parsing. =parse()= returns title, date, tags, bodyHtml, rawContent. 5 tests.
- *Phase 2* — Build pipeline. =build()= globs =*.org=, applies transforms, writes to =dist/=. 6 tests.
- *Phase 3* — Dev server + watch mode. chokidar v3 watcher; clean-URL static server. 3 tests.
- *Phase 4* — nginx config. Clean URLs, gzip, security headers. TLS via Cloudflare.
- *Phase 5* — DNS wiring. Hover A record → VPS. Site live at =amanuensiswake.com=.
- *Phase 6* — Transform pipeline. =transforms?: Transform[]= in BuildConfig; =visitNodes= utility. 4 tests.
- *Phase 7* — Content repo separation. =amanuensiswake-content= as public git submodule at =content/=.
- *Cloudflare* — Nameservers switched to Cloudflare. SSL/TLS Full (Strict). Proxying enabled.
- *CI/CD* — GitHub Actions workflow: push to content repo triggers build + rsync to VPS.
- *Styling* — =public/style.css= and =templates/base.ts= reconciled against =amanuensiswake-design-spec.org=.
  Design tokens (=--aw-*=), dark mode, Source Serif 4, two-column entry grid, pill tags,
  notes block, home/article layout variants, all components per spec.

* Deferred features
:PROPERTIES:
:STATUS: deferred
:END:

** Syntax highlighting

Add =rehype-highlight= or =shiki= to the pipeline for code blocks. Implement as
a transform. Low complexity, high payoff — good warm-up for the transform system.
Monospace typeface decision deferred to this phase (see design spec).

** Sidenotes

Convert Org footnotes (=[fn:1: text]= syntax) to margin notes in the rendered
HTML. Requires a sidenote-aware CSS layout and a transform that restructures
footnote AST nodes into sidenote HTML. The 640px column with generous margins
already anticipates this — no layout changes needed beyond sidenote-specific rules.
Tufte CSS is a useful reference.

** Backlinks

Collect all internal wiki-style links during a first build pass into a global
index. On a second pass, inject backlink sections into pages that are linked-to.
Requires a two-pass build and a persistent link index data structure.

** RSS feed

Emit =dist/feed.xml= from pages tagged =:published:= (or similar) that have a
=#+DATE= or =#+CREATED=. Implement as a post-build step rather than a transform.

** Colophon symbol

A visual mark for Amanuensis Wake to replace/accompany the footer text when
designed. One-element substitution in the footer template.

** Writings page

Deferred until enough content exists to warrant a non-chronological view
(by form or theme). Nav item stub exists.

** Support page

Nav item stub exists. Deferred until infrastructure is in place.

* Session handoff notes

When starting a new AI session with this plan:

1. Paste this file's contents as context.
2. Describe what you want to work on next.
3. Paste any relevant error output.

The canonical rule: tests must fail before code is written. If a task has no
automated tests, state that explicitly and describe the manual validation steps.
